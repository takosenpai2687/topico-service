server:
  port: 8081

spring:
  application:
    name: user
  datasource:
    url: jdbc:mysql://localhost:3306/topico
    username: root
    password: 078065
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # or other options like "create", "validate", "none"
    show-sql: true # Set to true to see SQL statements in logs
    database-platform: org.hibernate.dialect.MySQLDialect


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      fetch-registry: true
      register-with-eureka: true